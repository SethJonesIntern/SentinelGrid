version: "3.9"

services:
  frontend:
    build: ../HoneyNet-Frontend
    container_name: honeypot-frontend
    depends_on:
      - api
    environment:
      - API_URL=http://localhost:8000
    ports:
      - "3000:80"